{
  "name": "proyecto_congelados",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . spool-app --platform=win32 --arch=x64 --out=dist --overwrite",
    "package-win": "electron-packager . CongelamientoUsuarios --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico",
    "package-mac": "electron-packager . CongelamientoUsuarios --platform=darwin --arch=x64 --out=dist --overwrite --icon=assets/icon.icns"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^37.4.0",
    "electron-packager": "^17.1.2"
  }
}
